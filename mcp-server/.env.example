# Copy to .env if you need to override anything (optional — defaults work
# out of the box). No auth token needed; this only serves public docs.
# REPO_PATH=../docs is the default (relative to this compose file) — only
# set this if you want to point at a different checkout instead.
# REPO_PATH=/srv/i2t-docs-repo
